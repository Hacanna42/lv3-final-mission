spring:
  jpa:
    show-sql: false
    defer-datasource-initialization: true
    hibernate.ddl-auto: validate
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.0.0.190:3306/final?characterEncoding=UTF-8&serverTimezone=UTC
    username: app-prod
    password: "0000"

  sql:
    init.mode: never
